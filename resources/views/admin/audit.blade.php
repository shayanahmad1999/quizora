<div class="page-heading"><div><span class="eyebrow">A CLEAR RECORD</span><h1>Activity log</h1><p>Administrative changes and attempt lifecycle events. Passwords and answer keys are never recorded here.</p></div></div><form class="filter-bar" method="get" action="{{ route('admin.audit.index') }}" data-ajax data-filter><x-field name="q" label="Search activity" :value="request('q')" placeholder="Search action, entity, or description..."/>@include('shared.per-page')<button class="btn btn-primary" type="submit">Apply</button><a class="btn btn-light" data-nav href="{{ route('admin.audit.index') }}">Reset</a></form><section class="panel"><div class="table-wrap"><table class="data-table"><thead><tr><th>When</th><th>Actor</th><th>Action</th><th>Entity</th><th>Description</th></tr></thead><tbody>@forelse($logs as $log)<tr><td>{{ $log->created_at->timezone(config('app.display_timezone'))->format('d M Y, H:i') }}</td><td>{{ $log->user?->name ?? 'System' }}</td><td><span class="soft-badge">{{ ucfirst($log->action) }}</span></td><td>{{ $log->entity_type }} #{{ $log->entity_id }}</td><td>{{ $log->description }}</td></tr>@empty<tr><td colspan="5">No activity matches these filters.</td></tr>@endforelse</tbody></table></div><x-pagination :paginator="$logs"/></section>
