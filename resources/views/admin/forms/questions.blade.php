<div class="form-grid"><x-field name="category_id" label="Category" type="select" :options="[''=>'Select a category']+$categories->all()" :value="$record->category_id" :required="true"/><x-field name="difficulty" label="Difficulty" type="select" :options="['easy'=>'Easy','medium'=>'Medium','hard'=>'Hard']" :value="$record->difficulty" :required="true"/></div>
<x-field name="prompt" label="Question" type="textarea" :value="$record->prompt" rows="4" :required="true" maxlength="10000" help="Plain text only. Code snippets keep their line breaks and are displayed safely."/>
<div class="form-section-label">Answer choices</div><div class="form-grid">@foreach(['a','b','c','d'] as $key)<x-field :name="'option_'.$key" :label="'Option '.strtoupper($key)" :value="$record->options[$key] ?? ''" type="textarea" rows="2" :required="true" maxlength="2000"/>@endforeach</div>
<x-field name="correct_option" label="Correct answer" type="select" :options="[''=>'Select the correct option','a'=>'Option A','b'=>'Option B','c'=>'Option C','d'=>'Option D']" :value="$record->correct_option" :required="true"/>
<x-field name="explanation" label="Answer explanation" type="textarea" :value="$record->explanation" rows="3" maxlength="10000" help="Shown only after submission, when the quiz allows answer review."/>
<x-toggle name="is_active" label="Active question" :value="$record->is_active" help="Only active questions can be selected for new attempts."/>
